# V21 Update

Changes in `index.html` only:
- Added required Ahamove ownership/use-right commitment in Step 03.
- Replaced “tạo momentum” with “tạo đà”.

Deploy:
1. Replace root `index.html`
2. Commit to `main`
3. Push origin
4. Wait for Vercel auto-deploy
